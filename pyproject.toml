[project]
name = "category-theory"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "V0rvya", email = "28078533+V0RVYA@users.noreply.github.com" }
]
requires-python = ">=3.12"
dependencies = [
    "jupyter>=1.1.1",
]

[project.scripts]
category-theory = "category_theory:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "ipykernel>=7.3.0",
]
